# Phase 1 uses only local curated data (src/data/curatedDates.ts); no
# environment variables are required. This file exists to document the seam
# Phase 2 will use once api/ points at a real FastAPI backend, e.g.:
# VITE_API_BASE_URL=http://localhost:8000
